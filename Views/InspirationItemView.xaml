<UserControl
    x:Class="WhiteboardProjectBuilder.Views.InspirationItemView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewmodels="using:WhiteboardProjectBuilder.ViewModels"
    d:DataContext="{d:DesignInstance Type=viewmodels:InspirationItemViewModel}"
    mc:Ignorable="d">

    <Border BorderBrush="Black" BorderThickness="2" Width="600" Height="400">
        <Grid>
            <!-- Background Image -->
            <Image
                Source="{x:Bind ViewModel.Image, Mode=OneWay, Converter={StaticResource StringToImageSourceConverter}}"
                Stretch="UniformToFill" />

            <!-- Semi-transparent overlay for text readability -->
            <Border Background="#80FFFFFF" />

            <!-- Inspirational Text -->
            <TextBlock
                Text="{x:Bind ViewModel.Text, Mode=OneWay}"
                FontSize="24"
                FontWeight="SemiBold"
                TextWrapping="Wrap"
                TextAlignment="Center"
                VerticalAlignment="Center"
                HorizontalAlignment="Center"
                Padding="40"
                Foreground="Black" />
        </Grid>
    </Border>
</UserControl>
