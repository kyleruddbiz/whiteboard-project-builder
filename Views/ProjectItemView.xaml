<?xml version="1.0" encoding="utf-8" ?>
<UserControl
    x:Class="WhiteboardProjectBuilder.Views.ProjectItemView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewmodels="using:WhiteboardProjectBuilder.ViewModels"
    d:DataContext="{d:DesignInstance Type=viewmodels:ProjectItemViewModel}"
    mc:Ignorable="d">

    <Border BorderBrush="Black" BorderThickness="2" Width="400" Height="500">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <!-- Title and Subtitle -->
            <StackPanel Grid.Row="0" Padding="30 30 30 15">
                <TextBlock
                    Text="{x:Bind ViewModel.Title, Mode=OneWay}"
                    FontSize="32"
                    FontWeight="Bold"
                    TextAlignment="Center" />
                <TextBlock
                    Text="{x:Bind ViewModel.Subtitle, Mode=OneWay}"
                    FontSize="18"
                    TextAlignment="Center" />
            </StackPanel>

            <!-- Main Image -->
            <Image
                Grid.Row="1"
                Source="{x:Bind ViewModel.Image, Mode=OneWay, Converter={StaticResource StringToImageSourceConverter}}"
                Stretch="UniformToFill"
                Margin="30 0 30 15" />

            <!-- Size and Value Icons -->
            <Grid Grid.Row="2" Height="100" Margin="30 0 30 30">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <Border Grid.Column="0" Margin="0 0 7.5 0">
                    <Image Source="{x:Bind ViewModel.SizeIcon, Mode=OneWay, Converter={StaticResource StringToImageSourceConverter}}" Stretch="Uniform" />
                </Border>

                <Border Grid.Column="1" Margin="7.5 0 0 0">
                    <Image Source="{x:Bind ViewModel.ValueIcon, Mode=OneWay, Converter={StaticResource StringToImageSourceConverter}}" Stretch="Uniform" />
                </Border>
            </Grid>
        </Grid>
    </Border>
</UserControl>
